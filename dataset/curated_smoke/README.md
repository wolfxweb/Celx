# Smoke pt-BR (pipeline local)

Exemplos mínimos para validar LoRA no M1. **Não** usar como base de qualidade.

Gerado por:

```bash
python scripts/prepare_smoke_sft.py --config configs/train_local.yaml
```

A curadoria real fica em `dataset/curated/`.
